{
    "name": "Spatie role and permission management",
    "version": "1.0.0",
    "engines": {
        "php": ">=8.0.0"
    },
    "build": {
        "commands": ["composer install", "npm install", "npm run build"]
    },
    "routes": [
        {
            "src": "/public/*",
            "dest": "/"
        }
    ]
}
